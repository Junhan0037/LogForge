# Grafana 데이터소스 프로비저닝: Prometheus를 기본 데이터소스로 등록한다.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    version: 1
    editable: false
